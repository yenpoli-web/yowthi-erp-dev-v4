name: dotnet-self-hosted

on:
  push:
    branches:
      - 'm*-validation'
      - 'p*-validation'

permissions:
  contents: read

env:
  DOTNET_NOLOGO: '1'
  DOTNET_CLI_TELEMETRY_OPTOUT: '1'

jobs:
  environment:
    runs-on: [self-hosted, yowthi-erp-v2]
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Show .NET SDK
        shell: powershell
        run: |
          & 'C:\Program Files\dotnet\dotnet.exe' --info

  agent-build:
    runs-on: [self-hosted, yowthi-erp-v2]
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build Development Agent
        shell: powershell
        run: |
          & 'C:\Program Files\dotnet\dotnet.exe' build src/YowThi.DevelopmentAgent3/YowThi.DevelopmentAgent3.csproj --configuration Release --nologo

  supervisor-build:
    runs-on: [self-hosted, yowthi-erp-v2]
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build Runtime Supervisor
        shell: powershell
        run: |
          & 'C:\Program Files\dotnet\dotnet.exe' build src/YowThi.RuntimeSupervisor/YowThi.RuntimeSupervisor.csproj --configuration Release --nologo

  updater-build:
    runs-on: [self-hosted, yowthi-erp-v2]
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build Runtime Supervisor Updater
        shell: powershell
        run: |
          & 'C:\Program Files\dotnet\dotnet.exe' build src/YowThi.RuntimeSupervisorUpdater/YowThi.RuntimeSupervisorUpdater.csproj --configuration Release --nologo

  acceptance-tests:
    runs-on: [self-hosted, yowthi-erp-v2]
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run .NET acceptance tests
        shell: powershell
        run: |
          & 'C:\Program Files\dotnet\dotnet.exe' test src/YowThi.DotnetAcceptanceTests/YowThi.DotnetAcceptanceTests.csproj --configuration Release --nologo --verbosity normal
